// import { useSelector } from "react-redux";

// export const getLearningContent = () => {
//   const learning = useSelector((state) => state.learning);
//   return learning;
// };

export const getLearningContent = (state) => state.learning;
